<Page
    x:Class="Phileas.Views.Pages.LandingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Phileas.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Transparent">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Column="1"
                        Grid.Row="0"
                        DefaultLabelPosition="Right"
                        Margin="0,0,0,8">
            <AppBarButton x:Name="AppBarButton_OpenSelected" Icon="NewWindow" Label="Auswahl öffnen" IsEnabled="False" Visibility="Collapsed"/>
            <AppBarButton x:Name="AppBarButton_AddSimulation" Icon="Add" Label="Neue Simulation" Background="{StaticResource SystemAccentColor}" Foreground="{StaticResource SystemAltHighColor}" Click="AppBarButton_AddSimulation_Click"/>
            <AppBarButton Icon="OpenFile" Label="Datei öffnen"/>
            <CommandBar.Content>
                <TextBlock Text="Simulationen" FontSize="{StaticResource FontSize_Title}" Padding="12,4"/>
            </CommandBar.Content>
        </CommandBar>

        <ListView x:Name="ListView_Simulations" Grid.Row="1" Grid.ColumnSpan="2" Padding="0,0,12,0"
                  ItemClick="ListView_Simulations_ItemClick" IsItemClickEnabled="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding}" FontSize="{StaticResource FontSize_Subtitle}" Grid.Column="0"/>
                        <TextBlock Text="Letzte Bearbietung: 12.02.2021" Grid.Column="1"/>
                        <StackPanel Orientation="Horizontal" Grid.Column="2" Spacing="20">
                            <Grid Height="80" Width="120" Background="Silver"/>
                            <Grid Height="80" Width="120" Background="Silver"/>
                            <Grid Height="80" Width="120" Background="Silver"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <x:String>Simulation 1</x:String>
            <x:String>Simulation 2</x:String>
            <x:String>Simulation 3</x:String>
            <x:String>Simulation 4</x:String>
            <x:String>Simulation 5</x:String>
        </ListView>

    </Grid>
</Page>
