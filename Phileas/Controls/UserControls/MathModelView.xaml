<UserControl
    x:Class="Phileas.Controls.UserControls.MathModelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Phileas.Controls.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" OverflowButtonVisibility="Collapsed" IsDynamicOverflowEnabled="False" Margin="0,0,0,8">
            <CommandBar.Content>
                <TextBlock Text="Mathematisches Modell" FontSize="{StaticResource FontSize_Subtitle}" VerticalAlignment="Center" Margin="12,8,0,0" Height="40" Width="400"/>
            </CommandBar.Content>
            <AppBarButton x:Name="AppBarButton_AddExpression" Icon="Add" Label="Ausdruck" Click="AppBarButton_AddExpression_Click"/>
        </CommandBar>


        <ListView   x:Name="ListView_Expressions" 
                    SelectionMode="None"
                    ItemsSource="{x:Bind MathModel.Expressions, Mode=TwoWay}"
                    Grid.Row="1">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <AutoSuggestBox Text="{Binding Name, Mode=TwoWay}" Grid.Column="1" Margin="4"/>
                        <Button Content="=" HorizontalContentAlignment="Center" Grid.Column="2" Padding="4,0,4,4" Background="Transparent" HorizontalAlignment="Stretch" Margin="0,0,2,0">
                            <Button.Flyout>
                                <CommandBarFlyout>
                                    <AppBarButton x:Name="AppBarButton_DeleteMathModelExpression" Icon="Delete" Label="Ausdruck entfernen" Click="AppBarButton_DeleteMathModelExpression_Click"/>
                                </CommandBarFlyout>
                            </Button.Flyout>
                        </Button>
                        <TextBox Text="{Binding AssignmentExpression, Mode=TwoWay}" Grid.Column="3" Height="36" VerticalContentAlignment="Bottom"/>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <x:String></x:String>
        </ListView>

    </Grid>
</UserControl>
