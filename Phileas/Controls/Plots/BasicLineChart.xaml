<UserControl
    x:Class="Phileas.Controls.Plots.BasicLineChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Phileas.Controls.Plots"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:lvc="using:LiveCharts.Uwp"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="500"
    Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0" Background="Transparent" DefaultLabelPosition="Right">
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="AppBarButton_DeleteChart" Icon="Delete" Label="Löschen"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <lvc:CartesianChart x:Name="CartesienChart" Grid.Row="1" Series="{x:Bind SeriesCollection, Mode=OneWay}"/>

    </Grid>
</UserControl>
